# GAP Analysis — SRS.docx vs Codebase (CarePath)

วันที่วิเคราะห์: 2026-09-19
เทียบ `SRS.docx` (Requirement Specification, User Story Map, FR/NFR, Release Plan, API Contract) กับโค้ดจริงใน `backend/` (Django + DRF) และ `frontend/` (Next.js)

สรุปภาพรวมก่อน: **โครงสร้างข้อมูล (models) และ CRUD API ถูกสร้างไว้ครบตามผังระบบเกือบทั้งหมด** แต่ **"หัวใจ" ของฟีเจอร์ Must-Have หลายตัวยังไม่ทำงานจริง** — โดยเฉพาะ **การคำนวณเส้นทาง (routing/pathfinding)**, **การบังคับสิทธิ์ตามบทบาทที่ระดับ API**, และ **workflow ของเจ้าหน้าที่ประจำจุดในการ "ระบุจุดถัดไป"** ยังเป็นเพียงข้อมูลเปล่า (data-only) ที่ยังไม่มี logic ใช้งานจริง

สถานะที่ใช้: **✅ ทำแล้ว** / **🟡 ทำบางส่วน** / **❌ ยังไม่ทำ**

---

## 1. Functional Requirements (FR)

| ID | เรื่อง | สถานะ | รายละเอียด |
|---|---|---|---|
| FR-01 | จัดการผัง (อาคาร/ชั้น/จุด/เส้นเชื่อม) พร้อมระยะทาง เวลาเดิน ทิศทาง | ✅ | โมเดล `Building/Floor/Node/Edge` ครบ, CRUD API ครบ, มี editor ภาพบน Admin (`FloorMapSection.tsx`) ลากจุด/สร้างเส้นเชื่อม/คาลิเบรตสเกลได้ ระยะทาง (`distance_m`) คำนวณอัตโนมัติจากพิกัด |
| FR-02 | จัดการจุดบริการ (ประเภท/ตำแหน่ง/ช่องบริการ/เวลาเปิด) ย้ายอาคารได้โดยไม่กระทบแม่แบบ/ผู้ป่วย | ✅ | `Node(node_type=SERVICE_POINT)` มีฟิลด์ครบ, `TemplateStep` อ้างอิงด้วย node id จึงย้ายห้องได้โดยไม่กระทบแม่แบบ |
| FR-03 | ปิด/เปิดลิฟต์หรือเส้นเชื่อมชั่วคราว + เตือนก่อนบันทึกถ้าจุดใดเข้าไม่ถึง | ❌ | ไม่มี field เปิด/ปิด (`is_closed`) บน `Edge`/`Node` เลย และไม่มี logic ตรวจ reachability ใด ๆ — **ตั้งใจไม่ทำในงานนี้**: routing engine (FR-13) ตอนนี้มีแล้วและ `shortest_path()`/`RouteResult.reachable` พร้อมให้ FR-03 เรียกใช้ตรวจ reachability ได้ทันทีเมื่อมี field เปิด/ปิด แต่ field เปิด/ปิดเองและ UI เตือนยังไม่ได้ทำในรอบนี้ (คนละ gap item จาก FR-13) |
| FR-04 | สร้าง/แก้แม่แบบเส้นทาง: ลำดับขั้นตอน, ขั้นแรกเสมอ, เงื่อนไข "ต้องทำหลัง", ตรวจไม่ให้เกิดวงจร | 🟡 | `PathwayTemplate/TemplateStep` + `prerequisite_steps` (M2M ในแม่แบบเดียวกัน) ทำแล้ว และรองรับ fan-out/fan-in ได้จริง (ตรวจ prerequisite ตอน start step) แต่ **ไม่มีการตรวจ cycle** ใน `TemplateStepSerializer.validate()` — แม่แบบที่มี prerequisite วนกลับมาเป็นวงจรจะบันทึกได้เงียบ ๆ และทำให้ขั้นตอนนั้น start ไม่ได้ตลอดไป |
| FR-05 | เงื่อนไขเวลา ("ต้องเริ่มก่อน HH:MM") และขั้นตอนชนิด "รอ" (เช่น รอผล 60–90 นาที) | ❌ | ไม่มีฟิลด์เวลาบังคับหรือ step-type "รอ" ใน `TemplateStep`/`VisitStep` เลย (`ServiceSchedule.open_time/close_time` เป็นเวลาเปิด-ปิดจุดบริการ ไม่ใช่ deadline ต่อขั้นตอน) |
| FR-06 | ลงทะเบียนรายวัน: เลือกแม่แบบ/ไทย-ต่างชาติ/เดินได้-รถเข็น สร้างขั้นตอนอัตโนมัติ (1 Visit ที่ใช้งานอยู่ต่อคนต่อวัน) | 🟡 | ฟอร์มลงทะเบียน (`RegisterVisitForm.tsx`) ทำงานได้ครบ, สร้าง `VisitStep` snapshot อัตโนมัติจากแม่แบบ, มี checkbox รถเข็น (`uses_wheelchair`) แต่ "ไทย/ต่างชาติ" ถูกแทนด้วย `preferred_language` (th/en) ของผู้ป่วยแทน ไม่ใช่ flag สัญชาติตรง ๆ — ตอบโจทย์เรื่องภาษาได้ แต่ความหมายไม่ตรงตัวสเปก และยังไม่พบการบังคับ "1 Visit ที่ใช้งานอยู่ต่อคนต่อวัน" (ไม่มี unique constraint/validation ป้องกันลงทะเบียนซ้ำวันเดียวกัน) |
| FR-07 | ออกใบเวชระเบียนพร้อม QR ให้ผู้ป่วยดูสถานะตนเอง | 🟡 | `VisitQrCodeButton.tsx` สร้าง QR (`{origin}/visit/{qr_token}`) และเปิดหน้าต่างพิมพ์ได้ แต่ `qr_token` **ไม่หมดอายุเมื่อ visit เป็น COMPLETED/CANCELLED** (endpoint `visit_by_token` ไม่ตรวจ status ก่อนคืนข้อมูล) ซึ่งขัดกับที่ระบุไว้ใน `MODELS.md` เอง |
| FR-08 | แสดงขั้นตอนทั้งหมดพร้อมสีสถานะ, "ขั้นตอนที่ n จาก m", บอกจุดถัดไปพร้อมเหตุผลเมื่อยังระบุไม่ได้ | 🟡 | สีสถานะ (เทา/ฟ้า/ส้ม/เขียว/ขีดฆ่า) และ "จุดถัดไป" (`NextStepCard`) ทำแล้วครบ **และตอนนี้สีฟ้าเป็นของจริงแล้ว** — `PublicVisitStep.is_next` (จาก FR-19/21 ด้านล่าง) ถูกส่งผ่าน `serialize_public_visit` แล้ว, `StepTimeline.tsx` แยก PENDING ที่ถูกเจ้าหน้าที่ระบุแล้ว (สีฟ้า `--brand-blue`, ข้อความ "พร้อมแล้ว — เจ้าหน้าที่ระบุให้ไปจุดนี้ได้เลย") ออกจาก PENDING ธรรมดาที่ยังรอเจ้าหน้าที่ระบุ (สีเทาเดิม) ทั้งในมุมมองเดี่ยวและกลุ่มคู่ขนาน (`ParallelStepCard`) — ยืนยันผ่านเบราว์เซอร์จริงแล้ว แต่ยังไม่พบข้อความรูปแบบ "ขั้นตอนที่ n จาก m" ที่ชัดเจน และไม่มี "เหตุผล" เมื่อจุดถัดไปยังระบุไม่ได้ (เช่น "รอผลตรวจ") เพราะไม่มี step-type "รอ" (ดู FR-05) — สองจุดนี้ยังเป็น gap เดิม |
| FR-09 | แสดงคิว/เวลารอ, เลือกไปจุดใดก่อนได้เมื่อมีหลายจุด, แนะนำจุดเวลาจำกัด/คิวสั้นกว่า, ไม่โชว์ชื่อ/โรคคนอื่น | 🟡 | Queue widget แสดงหมายเลขคิว/จำนวนคนก่อนหน้าได้ (`QueueWidget.tsx`) และ NextStepOptionsList แสดงตัวเลือกคู่ขนานได้ แต่ **ไม่มี logic "แนะนำ" จุดที่ควรไปก่อน** (ไม่มีเวลาจำกัดให้เทียบ, ไม่เปรียบเทียบความยาวคิวระหว่างตัวเลือก) — ผู้ป่วยเห็นตัวเลือกแต่ระบบไม่ช่วยตัดสินใจ |
| FR-10 | แจ้งเตือนผู้ป่วยเมื่อใกล้ถึงคิว | ❌ | ไม่มี `Notification` model และไม่มีกลไก push/แจ้งเตือนใด ๆ ในโค้ด (หน้าผู้ป่วย polling ทุก 10 วินาทีเท่านั้น ไม่มี alert เชิงรุก) |
| FR-11 | เตือนเมื่อขั้นตอนเวลาจำกัดใกล้หมดเวลา (ทั้งฝั่งผู้ป่วยและฝั่งเจ้าหน้าที่) | ❌ | ขึ้นกับ FR-05 ที่ยังไม่มีฟิลด์เวลาบังคับ จึงไม่มีอะไรให้เตือน |
| FR-12 | นำทางเป็นคำสั่งเข้าใจง่าย (เลี้ยว/ลิฟต์/ชั้น) พร้อมระยะทาง/เวลาเดิน | ✅ | **แก้แล้ว** — `backend/facility/routing.py::shortest_path()` คืนลำดับ `RouteLeg` (แต่ละช่วง: `edge_type`, `distance_m`, `walk_time_sec`, `turn` ที่คำนวณจาก bearing สองช่วงติดกันบนชั้นเดียวกัน) ผ่าน `GET /api/facility/route`; ฝั่ง UI ใหม่ `frontend/components/portal/RouteInstructions.tsx` render เป็นรายการคำสั่งทีละขั้น ("เดินตรงไป X ม.", "เลี้ยวซ้าย/ขวา เดินต่อ X ม.", "ขึ้น/ลงลิฟต์ไปชั้น Y", "เดินขึ้น/ลงบันไดไปชั้น Y") พร้อมสรุประยะทาง/เวลารวมท้ายรายการ ใช้ในทั้ง Patient Portal และ Kiosk แทนเข็มทิศเดิม (`DirectionBlock`/`direction.ts` ยังเก็บไว้เป็น fallback เมื่อไม่มีข้อมูลเส้นทางจริง เช่น ยังไม่สแกนตำแหน่ง หรือ backend เข้าไม่ถึง) |
| FR-13 | คำนวณเส้นทางสั้นสุดจากฐานข้อมูลผัง (ไม่ฝังเส้นทาง) เคารพทิศทางเส้นเชื่อมและลิฟต์จอดบางชั้น | ✅ | **แก้แล้ว** — เพิ่ม Dijkstra ล้วน (`heapq`, ไม่ใช้ dependency ใหม่เพราะ `networkx` ไม่ได้ติดตั้งอยู่แล้ว) ใน `backend/facility/routing.py`, ถ่วงน้ำหนักด้วย `walk_time_sec` ตามสเปก, เคารพ `Edge.is_bidirectional` (เพิ่มทิศเดียวเมื่อ False), ข้าม floor ผ่าน `ELEVATOR`/`STAIRS` edge ที่เชื่อม `VERTICAL_CONNECTOR` ต่างชั้นได้ตรง ๆ (ไม่ต้องพิเศษเพราะกราฟไม่สนใจชั้นอยู่แล้ว) เปิดเผยผ่าน `GET /api/facility/route?from_node=&to_node=&wheelchair=` (`AllowAny` เพราะ Patient Portal/Kiosk ไม่มี session) ตรวจสอบด้วยข้อมูล seed จริงผ่าน Django shell ครบ 4 กรณี (เดินชั้นเดียวกัน, ข้ามชั้นผ่านลิฟต์/บันได, wheelchair หลีกบันได, เส้นทางที่ไปไม่ถึงจริง — คนละอาคาร OPD/ER) และผ่าน curl แบบไม่มี auth |
| FR-14 | ระบุตำแหน่งปัจจุบันด้วยสแกน QR (ไม่ใช้ GPS) | ✅ | `location_qr_code` บน `Node` + กล้องสแกน (`CameraScanner.tsx`/jsQR) หรือกรอกโค้ดเอง ใช้ได้ทั้งหน้าผู้ป่วยและ kiosk |
| FR-15 | แสดงแผนผังชั้นพร้อมลากเส้นทางที่ต้องเดิน | ❌ | **ยังไม่ทำ (ตั้งใจ)** — งานนำทาง (FR-12/13/17) ที่เพิ่งแก้ให้เฉพาะ "คำสั่งเดินเป็นข้อความ" (turn-by-turn) เท่านั้น ยังไม่ได้วาดเส้นทางทับภาพ floor plan จริง แผนผังชั้น (floor plan image) ยังมีเฉพาะใน **Admin editor** เพื่อวางจุด/เส้นเชื่อม ไม่ได้แสดงให้ผู้ป่วย/kiosk เห็นเลย งานนี้ (แสดงภาพ floor plan + วาดเส้นทาง) ยังเป็น gap แยกที่ต้องทำเพิ่ม |
| FR-16 | สลับ TH/EN ทั้งระบบ | ✅ | `next-intl` + `messages/th.json`/`en.json` ครบทั้ง Admin และ Patient/Kiosk (ภาษาตั้งต้นตาม `preferred_language` ของผู้ป่วย) |
| FR-17 | ขยายตัวอักษร + โหมดเลี่ยงบันไดอัตโนมัติสำหรับผู้ป่วยรถเข็น | 🟡 | **ครึ่งหนึ่งแก้แล้ว** — `visit.uses_wheelchair` ถูกส่งเป็น `wheelchair` param ไปยัง `GET /api/facility/route` แล้วจริง เมื่อ `wheelchair=true` routing engine จะตัด edge ที่ `wheelchair_accessible=False` ออกจากกราฟทั้งหมด (ตรวจแล้วด้วยข้อมูล seed จริง: เส้นทาง 4→23 สลับจาก `STAIRS` เป็น `ELEVATOR` โดยอัตโนมัติเมื่อ wheelchair=true) และยืนยันซ้ำผ่าน UI จริงในเบราว์เซอร์ (visit ผู้ป่วยรถเข็นได้เส้นทางผ่านลิฟต์ 3 ชั้นล้วน ไม่มีบันไดเลย) — ส่วนที่ยังไม่ทำคือ **ขยายตัวอักษร**: ยังไม่มีปุ่ม/การตั้งค่าขยายตัวอักษรที่ใดในระบบเลย (ยังเป็น gap เดิม) |
| FR-18 | คำนวณเส้นทางใหม่และแจ้งผู้ป่วยเมื่อเส้นทางถูกกระทบจากปิดลิฟต์/เส้นเชื่อม | ❌ | **ยังไม่ทำ (ตั้งใจ, นอก scope งานนี้)** — ขึ้นกับ FR-03 (ปิด/เปิดเส้นเชื่อม) ซึ่งยังไม่มี field `is_closed`/`is_open` บน `Edge`/`Node` เลย งานนี้แก้เฉพาะ routing engine (FR-13) ไม่ได้เพิ่มกลไกปิด/เปิดเส้นทางหรือ re-route อัตโนมัติเมื่อสภาพกราฟเปลี่ยน — FR-03 + FR-18 เป็น gap ถัดไปที่ต้องทำร่วมกัน |
| FR-19 | หน้าจอเจ้าหน้าที่ประจำจุด: ดูคิว, check-in, เรียกคิว/เรียกซ้ำ, บันทึกเสร็จ (รวมผลตรวจพร้อม), **ระบุจุดถัดไป**, ผู้ป่วยไม่มา, ข้ามขั้นตอน (ต้องระบุเหตุผล) | 🟡 | ดูคิว/เรียกคิวถัดไป/Serve/Done ทำแล้ว (`QueueConsolePage.tsx`) และ **"ระบุจุดถัดไป" แก้แล้ว** — เพิ่ม `VisitStep.is_next` (staff-designated eligibility, migration `visits/0002`+`0003` พร้อม data migration grandfather-in ข้อมูลเดิม), `VisitStepViewSet.complete`/`skip` คืน 409 พร้อม `eligible_next_steps` (รวม `waiting_count` ต่อจุด) เมื่อยังไม่ส่ง `next_step_ids`, ส่ง `next_step_ids` เพื่อระบุ/ปลดล็อกเฉพาะจุดที่เลือก; ฝั่ง Admin Portal (`VisitDetail.tsx` + `DesignateNextStepModal.tsx`) เปิด picker อัตโนมัติเมื่อเจอ 409 (ติ๊กทุกช่องเป็นค่าเริ่มต้นตาม FR-09) — ทดสอบผ่านเบราว์เซอร์จริงแล้วว่าเลือกแค่ตัวเลือกเดียวทำให้ตัวที่ไม่ได้เลือกยัง start ไม่ได้ ยังมี sub-gap ที่ตั้งใจไม่ทำในรอบนี้ 3 จุด: **(1) ไม่มี "เรียกซ้ำ" แยกจากเรียกคิวครั้งแรก**; **(2) "ผู้ป่วยไม่มา" ไม่มีสถานะ/ปุ่มแยกจาก skip ทั่วไป**; **(3) ปุ่ม Skip ไม่มีการบังคับ/บันทึกเหตุผล** (ไม่มีฟิลด์ reason ใน model ใดเลย) — สามข้อนี้ยังเป็น gap ที่ต้องทำต่อ |
| FR-20 | เพิ่มขั้นตอนกลางคัน (เช่น X-ray ปอด) พร้อมโชว์จำนวนคิวจุดนั้น โดยไม่ทำให้เงื่อนไขก่อนหลังเสีย | ✅ | **แก้แล้ว** — `POST /api/visits/visit-steps/{id}/insert-next` (`VisitStepViewSet.insert_next`) ให้เจ้าหน้าที่ ณ ขั้นตอนปัจจุบันของตนเองแทรก `VisitStep` ใหม่ (`is_planned=False`) ที่จุดบริการที่เลือก โดย prerequisite เดียวของขั้นตอนใหม่คือขั้นตอนปัจจุบัน และขั้นตอนที่ระบุใน `insert_before_step_ids` จะได้ขั้นตอนใหม่ "เพิ่ม" เข้าไปในเซ็ต prerequisite เดิม (ไม่แทนที่) จึงไม่ทำให้เงื่อนไขก่อนหลังเดิมเสีย — `sequence_order` ที่เหลือขยับให้เพื่อการแสดงผลเท่านั้น (ไม่ใช่ตัวบังคับลำดับจริง) ตอบกลับพร้อม `target_queue_waiting_count` (helper ใหม่ `queues/services.py::waiting_count_for_service_point`); ฝั่ง UI มีปุ่ม "เพิ่มขั้นตอนกลางคัน" + ฟอร์ม (`InsertUnplannedStepModal.tsx`) เลือกขั้นตอนปัจจุบัน/จุดบริการปลายทาง/ขั้นตอนที่ต้องรอก่อน แล้วโชว์จำนวนคิวรอเป็นข้อความยืนยัน — ทดสอบผ่าน curl และเบราว์เซอร์จริงครบ รวมถึงยืนยันว่าขั้นตอนใหม่ปรากฏใน `eligible_next_steps` ของ FR-19 ได้เองโดยไม่ต้องเขียน logic พิเศษเพิ่ม (เป็นผลพลอยได้จากกลไกเดียวกัน) — **บั๊กที่พบและแก้เพิ่มเติม:** เดิม `sequence_order` ของขั้นตอนใน `insert_before_step_ids` จะถูกขยับให้อยู่หลังขั้นตอนใหม่ก็ต่อเมื่อ `sequence_order` เดิมของมัน **มากกว่าหรือเท่ากับ** จุดที่แทรกอยู่แล้วเท่านั้น (โค้ดเดิม bump เฉพาะ `sequence_order__gte=new_sequence_order`) — กรณีที่ target อยู่คนละ branch และมี `sequence_order` **น้อยกว่า** จุดแทรก (เช่น เจ้าหน้าที่ยืนอยู่ที่ห้องตรวจแล้วแทรก "คลินิกทันตกรรม" ก่อน "ห้องการเงิน" ทั้งที่ห้องการเงินถูกวาง sequence ไว้ก่อนหน้าห้องตรวจในผังเดิม) prerequisite จะถูกผูกถูกต้อง (บังคับลำดับจริงทำงานถูก) แต่ **หน้าจอ Timeline ยังคงแสดงห้องการเงินอยู่ก่อนคลินิกทันตกรรมเหมือนเดิม** เพราะ `sequence_order` ของมันไม่เคยถูกขยับ ทำให้ดูเหมือนฟีเจอร์ไม่ทำงานตามที่ตั้งค่าไว้ — แก้โดยเพิ่ม loop ใน `insert_next` (`backend/visits/viewsets.py`) ให้ตรวจจับ target ที่ `sequence_order` เดิม ≤ ของขั้นตอนใหม่ แล้วดันไปต่อท้ายขั้นตอนใหม่ (พร้อมขยับขั้นตอนอื่นที่ชนกันให้เรียงต่อกันไป) มี regression test ใหม่ `visits/tests.py::InsertUnplannedStepOrderingTests` ยืนยันว่า fail ก่อนแก้และ pass หลังแก้ — **บั๊กที่พบและแก้เพิ่มเติม (รอบ 2):** เดิม `insert_next` ไม่ตรวจสถานะของ `current_step` เลยและปล่อยให้ `is_next` ของขั้นตอนใหม่เริ่มเป็น `False` เสมอโดยอาศัยว่า `complete`/`skip` ของ `current_step` จะมาตั้งค่าให้ทีหลังผ่านกลไก `next_step_ids` — แต่ถ้าเจ้าหน้าที่เลือก `current_step` ที่**เป็น `DONE`/`SKIPPED` ไปแล้ว** (เช่น ค่า default ของฟอร์ม `InsertUnplannedStepModal.tsx` ที่ตกไปที่ `steps[0]` เมื่อไม่มีขั้นตอนไหน `IN_PROGRESS` อยู่ ดู `VisitDetail.tsx`) ขั้นตอนนั้นจะไม่มีวันถูก complete/skip ซ้ำได้อีก (`complete`/`skip` ปฏิเสธสถานะที่ไม่ใช่ `IN_PROGRESS`/`PENDING`) กลไก `next_step_ids` จึงไม่มีวันทำงาน ทำให้ขั้นตอนใหม่ค้างเป็น `PENDING`/`is_next=False` ตลอดไป — หายไปจาก `next_steps` ของ `serialize_public_visit` (`backend/visits/views.py`) และทำให้หน้าผู้ป่วย/kiosk (`PatientPortalView.tsx`) ขึ้นข้อความ "รับบริการครบทุกขั้นตอนแล้ว" ทั้งที่ยังมีขั้นตอนค้างอยู่จริง — แก้โดยให้ `insert_next` ตั้ง `is_next=True` ให้ขั้นตอนใหม่ทันทีเมื่อ `current_step.status` เป็น `DONE`/`SKIPPED` อยู่แล้ว (prerequisite เดียวของมันถือว่าพร้อมตั้งแต่สร้าง) มี regression test ใหม่ `visits/tests.py::test_insert_on_already_done_step_is_immediately_designated` ยืนยันว่า fail ก่อนแก้และ pass หลังแก้ |
| FR-21 | ตรวจเงื่อนไขก่อนหลังตอนระบุจุดถัดไป, check-in ได้เฉพาะจุดที่ถูกระบุ | ✅ | **แก้แล้ว** — `VisitStepViewSet.start` เพิ่มเงื่อนไข `is_next=True` (400 `"This step has not been designated as the next step by staff yet."` ถ้ายังไม่ถูกระบุ) ควบคู่กับการตรวจ prerequisite เดิม (defense in depth, ยังคงไว้ไม่ลบ) — check-in (`start`) ทำได้เฉพาะจุดที่เจ้าหน้าที่ระบุแล้วจริง ตรงตาม FR ต้องการ; ขั้นตอนแรกของ pathway ถูก auto-designate **และ auto-start** ตอนลงทะเบียน (`VisitSerializer.create` เรียก `visits.services.start_step` ใหม่ — เป็นจุดเดียวกับที่ `VisitStepViewSet.start` ใช้) ทันทีที่ลงทะเบียนสำเร็จ ไม่ต้องกดเริ่มเองอีกต่อไป (เดิมแค่ designate แล้วเจ้าหน้าที่ต้องมากดเริ่มเอง) — visit เปลี่ยนเป็น `IN_PROGRESS` และมี `QueueTicket` ที่จุดแรกให้ทันที รองรับกรณี fan-out หลายขั้นตอนแรกพร้อมกันด้วย ทดสอบยืนยันแล้วว่า start บนขั้นตอนที่ prerequisite ผ่านแต่ยังไม่ถูกระบุจะ 400 ตามที่ต้องการ |
| FR-22 | Login+สิทธิ์ตามบทบาท (hash, prepared statement) + ผู้ป่วยเห็นเฉพาะข้อมูลตน | 🟡 | Hash รหัสผ่านและใช้ Django ORM ล้วน (ไม่มี raw SQL) ทำถูกต้อง — **แก้แล้ว:** เพิ่ม `RoleRequired` permission class (`backend/accounts/permissions.py`) และตั้ง `read_roles`/`write_roles` ตามตาราง Role ของสเปกให้ทุก `ModelViewSet`/`ReadOnlyModelViewSet` ใน `accounts`, `facility`, `pathway`, `visits`, `queues` แล้ว, เปลี่ยน `DEFAULT_PERMISSION_CLASSES` เป็น `IsAuthenticated` (`config/settings.py`) เป็น fail-safe backstop, และ endpoint ที่ตั้งใจให้ public (login/logout/me/health/kiosk lookup/visit-by-token/visit-by-hn-today/location-node-by-qr) ยังคง `AllowAny` ตามเดิม (ผู้ป่วยเข้าถึงข้อมูลตนผ่าน token เหล่านี้ ไม่ใช่ session login จึงไม่ได้แก้ในรอบนี้). ยังมี simplification ที่ตั้งใจปล่อยไว้ 2 จุด: (1) สเปกบางแถวให้ role ต่างกันระหว่าง `list` กับ `retrieve` ของ resource เดียวกัน (เช่น `GET /api/visits/visits` ให้ REGISTRAR+EXECUTIVE แต่ `GET .../{id}` ให้ REGISTRAR อย่างเดียว) — `RoleRequired` ปฏิบัติ list/retrieve เหมือนกันหมดโดยเจตนา ไม่ถือเป็นช่องโหว่ที่มีผลจริง; (2) ยังไม่ทำ per-service-point scoping (SERVICE_STAFF เห็นคิวของทุกจุดบริการ ไม่ใช่เฉพาะจุดที่ตนสังกัดผ่าน `ServicePointStaff`) — เป็น role-level gating เท่านั้น ยังไม่ scope ลงไปถึงระดับ object |
| FR-23 | บันทึก Audit ทุกการเปลี่ยนสถานะ: ใคร/เมื่อไร/จากสถานะใด→เป็นสถานะใด (เวลาระบบ) | 🟡 | มี `AuditLog` + `log_action()` จริง ครอบคลุม LOGIN/LOGOUT, CREATE/UPDATE/DELETE staff user, START/COMPLETE/SKIP visit step — แต่ **ไม่มีฟิลด์ from_status/to_status แยกชัดเจน** (เก็บเป็น JSON `detail` อิสระ ไม่ครบทุกครั้ง) และ **ไม่ครอบคลุม** การแก้ไข Facility/Pathway/ServiceSchedule/ServicePointStaff, Patient/Visit CRUD, และปุ่ม serve/done ในหน้า Queue Console (เรียก `complete_step` แต่ log เฉพาะบางเส้นทาง) |
| FR-24 | Dashboard ผู้บริหาร: เวลารอเฉลี่ยรายจุด, จุดคอขวด, เวลาที่ผู้ป่วยอยู่ รพ. เฉลี่ย (ไม่ระบุตัวผู้ป่วย) | ✅ | **แก้แล้ว** — เพิ่ม aggregation จริงฝั่ง backend แทนการคำนวณฝั่ง client ทั้งหมด: `Visit.completed_at` (ตั้งค่าใน `visits.services.sync_visit_completion` เมื่อ visit เข้าสถานะ `COMPLETED`, มี migration `visits/migrations/0004_visit_completed_at.py`), `GET /api/queues/stats/wait-times` (`queues/services.py::service_point_wait_stats` — ใช้ Django `Avg`/`Count` aggregate ต่อ `SERVICE_POINT` node จาก `VisitStep` ที่ `status=DONE` และมี `started_at`/`completed_at` ครบ ของวันที่ระบุ เรียงจาก `avg_minutes` มากไปน้อยเพื่อให้ index 0 คือจุดคอขวด — ทดสอบยืนยันแล้วว่า Django `Avg()` ของนิพจน์ลบ datetime ทำงานถูกต้องบน SQLite ของโปรเจกต์นี้จริง ไม่ต้อง fallback เป็น Python loop), และ `GET /api/visits/stats/total-time?days=N` (`visits/views.py::total_time_stats` — ค่าเฉลี่ย `completed_at - created_at` ของ visit ที่ `COMPLETED` ใน N วันล่าสุด) ทั้งสอง endpoint จำกัดสิทธิ์ด้วย permission factory ใหม่ `accounts/permissions.py::allowed_roles()` (สำหรับ `@api_view` ที่ไม่มี `read_roles`/`write_roles`/`action` แบบ ViewSet) ให้ EXECUTIVE เท่านั้น (ADMIN bypass ตามปกติ) — ทดสอบผ่าน curl ครบ 4 เคส (ไม่ login → 401/403, REGISTRAR/SERVICE_STAFF → 403, EXECUTIVE/ADMIN → 200) ฝั่ง `/admin/reports` (`ReportsPage.tsx`) เพิ่มการ์ด "เวลาที่ผู้ป่วยอยู่ รพ. เฉลี่ย" และกราฟแท่งแนวนอน "เวลารอเฉลี่ยรายจุด" พร้อม badge "จุดคอขวด" ที่จุดอันดับ 1 (ข้อมูลจริงจากทั้งสอง endpoint ข้างต้น re-fetch `total-time` ทุกครั้งที่เปลี่ยนช่วงวันที่) และแก้ **ปัญหาชื่อ-นามสกุลผู้ป่วยรั่วในหน้า `/admin` Dashboard** ที่ Executive เข้าถึงได้ทางสถาปัตยกรรม ไม่ใช่แค่ซ่อน UI — `app/admin/(authenticated)/page.tsx` ส่ง `currentUserRole` (จาก `getCurrentStaffUser()` ฝั่ง server) ให้ `DashboardPage`, ซึ่งสำหรับ role `EXECUTIVE` จะ**ไม่เรียก `patientsApi.list()` เลย** (ไม่ใช่แค่ไม่แสดงผล) และแสดง "การมาวันนี้แยกตามสถานะ" แทนตาราง "รายการเข้ารับบริการล่าสุด" ที่มีชื่อ/HN — ยืนยันด้วยการ login จริงเป็น EXECUTIVE ในเบราว์เซอร์แล้วตรวจ network requests ว่าไม่มี call ไปยัง `/api/visits/patients` เลย และ login เป็น ADMIN ยืนยันว่ายังเห็นชื่อผู้ป่วยตามเดิม (ฟีเจอร์นี้เฉพาะ EXECUTIVE เท่านั้น ไม่ใช่ลบออกทั้งระบบ); เพิ่ม `try/catch` รอบ `loadDashboardData()` ใน `DashboardPage.tsx` (เดิมมีแค่ `finally` ไม่มี `catch` — ถ้า fetch ใดใน `Promise.all` reject หน้าจะค้างที่ `loading` ตลอดไปแบบไม่มี error โชว์เลย) ให้ error ใด ๆ ในอนาคตแสดงสถานะ error ที่มองเห็นได้แทน — **บั๊ก RBAC-กับ-reporting ที่พบและแก้ระหว่างทำข้อนี้ (ดูหัวข้อด้านล่าง)** |
| FR-25 (Could/Release 1.1) | จัดลำดับขั้นตอนใหม่อัตโนมัติเมื่อคิวยาวผิดปกติ | ❌ | ไม่มีเลย (ตามคาด เพราะเป็น Release 1.1) |
| FR-26 (Could/Release 1.1) | ญาติติดตามผ่านลิงก์อายุจำกัด | ❌ | ไม่มีเลย |
| FR-27 (Could/Release 1.1) | อ่านคำสั่งนำทางเป็นเสียง | ❌ | ไม่มีเลย |
| FR-28 (Could/Release 1.1) | แนะนำจุดพัก/ห้องน้ำ/ร้านอาหารระหว่างทาง | ❌ | ไม่มีเลย |

### บั๊ก RBAC/reporting ที่พบและแก้ระหว่างทำ FR-24

พบจากการ login จริงเป็น **EXECUTIVE** (ไม่ใช่แค่ ADMIN ซึ่ง bypass ทุก role check ใน `RoleRequired` และจะซ่อนบั๊กประเภทนี้ไว้ได้สนิท) แล้วเดินหน้า `/admin` และ `/admin/reports` จริงในเบราว์เซอร์ ระหว่างทำ FR-24 พบว่า item 1 (RBAC ระดับ permission layer, commit `20a3458`) ตั้ง `read_roles` ของหลาย ViewSet แคบตาม "เจ้าของข้อมูลหลัก" ของแต่ละ resource โดยไม่ได้ตรวจว่าหน้า Dashboard/Reports ที่เปิดให้หลาย role เข้าถึงพร้อมกัน (ดู `admin-nav.ts`) ต้องอ่านข้อมูลข้าม-resource เหล่านี้ด้วย แก้ไปทั้งหมด 4 จุด (ทุกจุดเป็นการขยายสิทธิ์**อ่านอย่างเดียว**, `write_roles` ไม่แตะ):

1. `VisitStepViewSet.read_roles` (`backend/visits/viewsets.py`) เดิมมีแค่ `(SERVICE_STAFF,)` ไม่รวม `EXECUTIVE` — ทำให้ `ReportsPage.tsx`'s `listVisitSteps()` (ใช้คำนวณ "จำนวนขั้นตอนเฉลี่ยต่อ visit") 403 สำหรับ Executive แก้โดยขยายเป็น `(SERVICE_STAFF, EXECUTIVE)` — `write_roles` ยังคง `SERVICE_STAFF` เท่านั้น (Executive ยัง start/complete/skip/insert ขั้นตอนไม่ได้) เหตุผล: Executive มีสิทธิ์อ่าน `Visit`/`Queue` อยู่แล้วในสคีมาเดียวกันนี้ การอ่าน `VisitStep` เพิ่มจึงเป็นการขยายสิทธิ์ที่สมเหตุสมผล ไม่ใช่การถอย security ของข้อ 1
2. `NodeViewSet.read_roles` (`backend/facility/viewsets.py`) เดิมเป็น `()` (ADMIN เท่านั้น) — ทำให้ **ทุก role ที่ไม่ใช่ ADMIN** (รวม REGISTRAR/SERVICE_STAFF ที่ใช้หน้า `/admin` Dashboard ได้ตามปกติอยู่แล้ว) 403 บน `nodesApi.list()` ซึ่ง `DashboardPage.tsx` เรียกแบบไม่มีเงื่อนไขทุก role แก้โดยขยายเป็น `(REGISTRAR, SERVICE_STAFF, EXECUTIVE)` — `write_roles` ยังคง `()` (แก้ไขผังสถานที่ยังเป็นสิทธิ์ ADMIN เท่านั้นเหมือนเดิม ตรงกับเมนู "ผังสถานที่" ที่ ADMIN เท่านั้นเข้าถึงได้)
3. `PathwayTemplateViewSet.read_roles` (`backend/pathway/viewsets.py`) เดิมมีแค่ `(REGISTRAR,)` — ทำให้ `pathwayTemplatesApi.list()` ที่ `ReportsPage.tsx` เรียก (สำหรับกราฟ "แยกตามแม่แบบเส้นทาง") 403 สำหรับ Executive แก้โดยขยายเป็น `(REGISTRAR, EXECUTIVE)` — `write_roles` ยังคง `()`
4. `QueueTicketViewSet.read_roles` (`backend/queues/viewsets.py`) เดิมมีแค่ `(SERVICE_STAFF,)` — ทำให้ `listQueueTickets()` ที่ `DashboardPage.tsx` เรียกแบบไม่มีเงื่อนไขทุก role (คำนวณสถิติ "กำลังรอคิว") 403 สำหรับทั้ง REGISTRAR และ EXECUTIVE แก้โดยขยายเป็น `(SERVICE_STAFF, REGISTRAR, EXECUTIVE)` — `QueueTicketSerializer` ไม่มีข้อมูลผู้ป่วยเลย (มีแค่ id/queue/visit_step/ticket_number/status/called_at) จึงเป็นการขยายสิทธิ์อ่านที่ปลอดภัย; `write_roles` (serve/done) ยังคง `SERVICE_STAFF` เท่านั้น

**ตามมาแก้เพิ่ม (นอกรอบ agent ทั้ง 4):** `ServiceScheduleViewSet` (`read_roles=()`, ตรงตาม Role column ของสเปก API Contract ที่ระบุ ADMIN เท่านั้นสำหรับ `/api/queues/service-schedules`) ทำให้ SERVICE_STAFF ที่เห็นเมนู "ตารางเวลาบริการ" ในซะบาร์ (จาก `admin-nav.ts` เดิมที่เขียน `roles: ["SERVICE_STAFF","ADMIN"]` มาก่อนงาน RBAC รอบนี้) กด 403 ทุกครั้ง เพราะ nav ฝั่ง frontend ไม่เคยตรงกับ role ที่สเปกกำหนดไว้จริง — เลือกแก้ที่ **nav** ให้เหลือ `roles: ["ADMIN"]` (ตรงกับ backend/สเปก) แทนที่จะขยาย `write_roles` ของ backend ให้ SERVICE_STAFF แก้ตารางเวลาได้ เพราะสเปกตั้งใจให้เป็นสิทธิ์ ADMIN เท่านั้น ไม่ใช่บั๊กของ backend

---

## 2. Non-Functional Requirements (NFR)

| ID | เรื่อง | สถานะ | รายละเอียด |
|---|---|---|---|
| NFR-01 | Hash password, Prepared Statement, ตรวจสิทธิ์ทุก API | 🟡 | Hash ✅ / ORM (ไม่มี raw SQL) ✅ / **"ตรวจสิทธิ์ทุก API" แก้แล้ว** — ทุก ViewSet ตรวจสิทธิ์ผ่าน `RoleRequired` (`backend/accounts/permissions.py`) และ default ที่เหลือ (ถ้ามีใครลืมตั้ง) เป็น `IsAuthenticated` ไม่ใช่ `AllowAny` แล้ว ดูรายละเอียด simplification ที่เหลือใน FR-22 |
| NFR-02 | Audit ทุกการเปลี่ยนสถานะสำคัญ แก้/ลบไม่ได้ | 🟡 | มี audit log แต่ยังไม่ครอบคลุมทุก action (ดู FR-23) และ "แก้/ลบไม่ได้" เป็นเพียง **convention** (ไม่เรียก update/delete ในโค้ด) ไม่มี DB constraint/trigger บังคับจริง — ผู้ที่เข้าถึง DB โดยตรงยังลบ/แก้ได้ |
| NFR-03 | ใช้งานง่ายไม่ต้องอบรม, error message เข้าใจง่าย, ไม่โชว์ error code ดิบ | ⚪ ไม่ได้ตรวจสอบในรอบนี้ | ต้องทดสอบ UI จริงเพื่อยืนยัน ยังไม่ได้ตรวจ error-handling ของทุก endpoint |
| NFR-04 | ขยายตัวอักษรได้, contrast ≥ 4.5:1, ใช้บนจอ 320px ได้, ไม่ใช้สีอย่างเดียวบอกสถานะ | ❌ | ไม่มีปุ่ม/กลไกขยายตัวอักษรหรือโหมด high-contrast เลย, ไม่มีการตรวจ/ยืนยันอัตราส่วนสี, สถานะบางจุด (เช่น timeline) ใช้สีคู่กับ label ข้อความอยู่บ้าง (ผ่านเกณฑ์ "ไม่ใช้สีอย่างเดียว" บางส่วน) แต่ไม่ครบทุกจุดและไม่มีการทดสอบจอ 320px |
| NFR-05 | หน้าจอหลักแสดงผลภายใน 3 วินาที | ⚪ ไม่ได้ตรวจสอบในรอบนี้ | มีความเสี่ยง: Dashboard/Reports ใช้ pattern ดึงข้อมูลทีละ queue (N+1) และคำนวณฝั่ง client จาก list ทั้งหมด — ควรวัดจริงเมื่อข้อมูลจำลองมีขนาดใกล้เคียงของจริง |
| NFR-06 | Responsive ทั้งมือถือ/แท็บเล็ต/คอมพิวเตอร์ | 🟡 | หน้าผู้ป่วย/Kiosk เป็น layout กว้างคงที่ (`max-w-[430px]`) ใช้งานบนมือถือได้จริงแต่ไม่ปรับตาม breakpoint จริง ๆ; **ฝั่ง Admin เป็น desktop-only** (sidebar กว้างคงที่ ไม่มี responsive collapse) — ถ้าเจ้าหน้าที่ประจำจุดต้องใช้แท็บเล็ต/มือถือจะมีปัญหาการใช้งาน |
| NFR-07 | DB เชิงสัมพันธ์ถึง 3NF, แยก Frontend/Backend ผ่าน REST API มีเอกสาร endpoint+ตัวอย่าง | ✅ | Django ORM + DRF, Next.js แยกส่วนชัดเจนผ่าน REST, มี `drf-spectacular` (Swagger UI/Redoc) เป็นเอกสาร API พร้อมตัวอย่าง โครงสร้างโมเดลดูเป็น normalized form ที่สมเหตุสมผล |
| NFR-08 | อธิบายแนวทางสำรอง/กู้คืนข้อมูลได้ (ไม่ต้องทำงานจริง) | ❌ | ไม่พบเอกสารเรื่อง backup/restore ในโปรเจกต์เลย (ตรวจ README, IMPLEMENT_PLAN.md, seed/README.md, CREDENTIALS.md แล้วไม่มี) |
| NFR-09 | ใช้ Git commit สม่ำเสมอ, ใช้ข้อมูลจำลองเท่านั้น, ระบุการใช้ AI ในรายงาน | 🟡 | มี git history ต่อเนื่อง (`git log`) และมีชุด seed data จำลอง (`seed/*.csv` + loader) ตรงตามเจตนา; "ระบุการใช้ AI ในรายงาน" เป็นเนื้อหาที่ต้องเขียนแยกในรายงานส่ง ไม่ใช่สิ่งที่ตรวจจากโค้ดได้ |

---

## 3. API Contract (หมวด `4. Requirements` และเอกสาร API Contract)

โครงสร้างแอป (`accounts`, `facility`, `pathway`, `visits`, `queues`, `health`) และ endpoint ส่วนใหญ่ตรงกับที่ระบุในสเปกจริง (CRUD ครบตามลิสต์ endpoint, มี `by-token`/`by-hn-today` สำหรับผู้ป่วย/kiosk, มี `call-next`/`serve`/`done` สำหรับคิว) แต่มีความคลาดเคลื่อนหลักคือ:

- ~~คอลัมน์ "Role" ในเอกสาร (เช่น ADMIN, REGISTRAR, SERVICE_STAFF, EXECUTIVE) เป็นเพียงเจตนาการออกแบบ ไม่ได้ถูก enforce จริงที่ backend~~ **แก้แล้ว** — ทุก ViewSet ตั้ง `read_roles`/`write_roles` ตามคอลัมน์ Role ของสเปกและตรวจผ่าน `RoleRequired` (`backend/accounts/permissions.py`) แล้ว ดูรายละเอียด/simplification ที่เหลือใน FR-22/NFR-01
- ~~Endpoint สำหรับ "ระบุจุดถัดไปโดยเจ้าหน้าที่" (ตามที่ FR-19/20/21 ต้องการ) ไม่มีอยู่ในเอกสาร API Contract และไม่มีในโค้ดจริงด้วย~~ **แก้แล้ว** — เพิ่ม `next_step_ids` บน `VisitStepViewSet.complete`/`skip` (409 preview + designate) และ `POST /api/visits/visit-steps/{id}/insert-next` (ขั้นตอนกลางคัน) พร้อม `drf-spectacular` schema (`inline_serializer`) ครบ ดูรายละเอียดที่ FR-19/20/21
- ~~ไม่มี endpoint ใดเกี่ยวกับ routing/pathfinding (สมเหตุสมผล เพราะ engine นี้ยังไม่มี — ดู FR-13)~~ **แก้แล้ว** — เพิ่ม `GET /api/facility/route` (`AllowAny`) ที่รัน Dijkstra จริงและคืนระยะทาง/เวลา/เลี้ยว/ชั้นครบ ดูรายละเอียดที่ FR-13
- `GET /api/health` มีจริง (`core/views.py`, `core/urls.py`)

---

## 4. ส่วนที่เป็นไดอะแกรม/รูปภาพในเอกสาร (ตรวจไม่ได้จากข้อความ)

หัวข้อต่อไปนี้ในเอกสารมีเฉพาะหัวเรื่อง ไม่มีเนื้อหาข้อความให้ดึงออกมาได้ (คือรูปภาพ/diagram ที่ฝังไว้): **Use Case Diagram, ER Diagram, UX/UI Wireframe/Mockup, Test Case** — ควรเปิดไฟล์ต้นฉบับดูภาพเพื่อเทียบเพิ่มเติม โดยเฉพาะ **Test Case** ซึ่งควรใช้ตรวจว่ามี automated/manual test coverage ในโค้ดตรงกันหรือไม่ (จากการสำรวจโค้ด ยังไม่พบชุดทดสอบอัตโนมัติที่ครอบคลุม flow หลักในโปรเจกต์)

---

## 5. เรียงลำดับความสำคัญที่ควรแก้ก่อน (แนะนำ)

1. ~~บังคับสิทธิ์ตามบทบาทที่ระดับ API (FR-22 / NFR-01)~~ **แก้แล้ว** — ดูรายละเอียดใน FR-22/NFR-01
2. ~~**Pathfinding engine (FR-13)** — เป็นฐานของฟีเจอร์นำทางแทบทั้งหมด (FR-03, 11, 12, 15, 17, 18) ถ้าไม่ทำ ฟีเจอร์นำทางที่ทำได้จริงจะเหลือแค่เข็มทิศ ไม่ตรงกับสเปก M5/M6~~ **แก้แล้ว** — เพิ่ม Dijkstra shortest-path engine (`backend/facility/routing.py` + `GET /api/facility/route`) และ turn-by-turn UI (`RouteInstructions.tsx`) แล้ว ปลดล็อก FR-12/13/17(ครึ่งหนึ่ง) ที่เหลือ **FR-03 (ปิด/เปิดเส้นเชื่อม), FR-15 (วาดเส้นทางทับ floor plan image), FR-18 (re-route อัตโนมัติเมื่อปิดเส้นทาง), และครึ่งขยายตัวอักษรของ FR-17** ยังเป็น gap ถัดไปที่ต้องทำ (FR-03+FR-18 ควรทำคู่กันเพราะ FR-18 ขึ้นกับ FR-03 ตรง ๆ)
3. ~~**Workflow "เจ้าหน้าที่ระบุจุดถัดไป" + เพิ่มขั้นตอนกลางคัน (FR-19–21, FR-20)** — เป็น M7 Must Have ที่ตอนนี้ยังไม่มี UI/API เลย เป็น core flow ของสถานการณ์ตัวอย่างในเอกสาร (Flow Diagram) ทั้งหมด~~ **แก้แล้ว** — `VisitStep.is_next` + `complete`/`skip`'s `next_step_ids` (409 preview → designate) + `POST .../insert-next` (ขั้นตอนกลางคันพร้อมโชว์คิว) ทั้ง backend/frontend ดูรายละเอียดที่ FR-19/20/21 (และ FR-08 สำหรับสีฟ้าฝั่งผู้ป่วย) ที่เหลือเป็น sub-gap ตั้งใจไม่ทำตามที่ระบุใน FR-19: เรียกซ้ำ, no-show แยกจาก skip, skip-with-reason
4. ~~**Dashboard แยกรายจุด + ซ่อนชื่อผู้ป่วยจากมุมมอง Executive (FR-24)**~~ **แก้แล้ว** — เพิ่ม backend aggregation จริง (`GET /api/queues/stats/wait-times`, `GET /api/visits/stats/total-time`) และปิดการรั่วของชื่อ-นามสกุลผู้ป่วยจาก Dashboard ฝั่ง Executive ทางสถาปัตยกรรม (ไม่เรียก `patientsApi.list()` เลยสำหรับ role นี้) พร้อมแก้บั๊ก RBAC-กับ-reporting 4 จุดที่พบระหว่างทดสอบจริงเป็น EXECUTIVE ดูรายละเอียดที่ FR-24
5. ~~ตรวจสอบ `ServiceScheduleViewSet`/`/admin/schedule` สำหรับ SERVICE_STAFF~~ **แก้แล้ว** — แก้ที่ `frontend/lib/admin-nav.ts` (`roles: ["ADMIN"]` แทน `["SERVICE_STAFF","ADMIN"]`) ให้ตรงกับ backend/สเปกที่ตั้งใจให้เป็นสิทธิ์ ADMIN เท่านั้นอยู่แล้ว ไม่ใช่ backend bug
6. Cycle detection บนแม่แบบ, skip-with-reason, no-show แยกจาก skip, audit ให้ครอบคลุมทุก action, backup/restore doc, accessibility (font-scale/contrast), FR-03+FR-18 (ปิด/เปิดเส้นเชื่อม + re-route อัตโนมัติ), FR-15 (วาดเส้นทางทับ floor plan image)
