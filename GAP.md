# GAP Analysis — SRS.docx vs Codebase (CarePath)

วันที่วิเคราะห์: 2026-09-19
เทียบ `SRS.docx` (Requirement Specification, User Story Map, FR/NFR, Release Plan, API Contract) กับโค้ดจริงใน `backend/` (Django + DRF) และ `frontend/` (Next.js)

สรุปภาพรวมก่อน: **โครงสร้างข้อมูล (models) และ CRUD API ถูกสร้างไว้ครบตามผังระบบเกือบทั้งหมด** แต่ **"หัวใจ" ของฟีเจอร์ Must-Have หลายตัวยังไม่ทำงานจริง** — โดยเฉพาะ **การคำนวณเส้นทาง (routing/pathfinding)**, **การบังคับสิทธิ์ตามบทบาทที่ระดับ API**, และ **workflow ของเจ้าหน้าที่ประจำจุดในการ "ระบุจุดถัดไป"** ยังเป็นเพียงข้อมูลเปล่า (data-only) ที่ยังไม่มี logic ใช้งานจริง

สถานะที่ใช้: **✅ ทำแล้ว** / **🟡 ทำบางส่วน** / **❌ ยังไม่ทำ**

---

## 1. Functional Requirements (FR)

| ID | เรื่อง | สถานะ | รายละเอียด |
|---|---|---|---|
| FR-01 | จัดการผัง (อาคาร/ชั้น/จุด/เส้นเชื่อม) พร้อมระยะทาง เวลาเดิน ทิศทาง | ✅ | โมเดล `Building/Floor/Node/Edge` ครบ, CRUD API ครบ, มี editor ภาพบน Admin (`FloorMapSection.tsx`) ลากจุด/สร้างเส้นเชื่อม/คาลิเบรตสเกลได้ ระยะทาง (`distance_m`) คำนวณอัตโนมัติจากพิกัด |
| FR-02 | จัดการจุดบริการ (ประเภท/ตำแหน่ง/ช่องบริการ/เวลาเปิด) ย้ายอาคารได้โดยไม่กระทบแม่แบบ/ผู้ป่วย | ✅ | `Node(node_type=SERVICE_POINT)` มีฟิลด์ครบ, `TemplateStep` อ้างอิงด้วย node id จึงย้ายห้องได้โดยไม่กระทบแม่แบบ |
| FR-03 | ปิด/เปิดลิฟต์หรือเส้นเชื่อมชั่วคราว + เตือนก่อนบันทึกถ้าจุดใดเข้าไม่ถึง | ❌ | ไม่มี field เปิด/ปิด (`is_closed`) บน `Edge`/`Node` เลย และไม่มี logic ตรวจ reachability ใด ๆ — เพราะไม่มี routing engine (ดู FR-13) |
| FR-04 | สร้าง/แก้แม่แบบเส้นทาง: ลำดับขั้นตอน, ขั้นแรกเสมอ, เงื่อนไข "ต้องทำหลัง", ตรวจไม่ให้เกิดวงจร | 🟡 | `PathwayTemplate/TemplateStep` + `prerequisite_steps` (M2M ในแม่แบบเดียวกัน) ทำแล้ว และรองรับ fan-out/fan-in ได้จริง (ตรวจ prerequisite ตอน start step) แต่ **ไม่มีการตรวจ cycle** ใน `TemplateStepSerializer.validate()` — แม่แบบที่มี prerequisite วนกลับมาเป็นวงจรจะบันทึกได้เงียบ ๆ และทำให้ขั้นตอนนั้น start ไม่ได้ตลอดไป |
| FR-05 | เงื่อนไขเวลา ("ต้องเริ่มก่อน HH:MM") และขั้นตอนชนิด "รอ" (เช่น รอผล 60–90 นาที) | ❌ | ไม่มีฟิลด์เวลาบังคับหรือ step-type "รอ" ใน `TemplateStep`/`VisitStep` เลย (`ServiceSchedule.open_time/close_time` เป็นเวลาเปิด-ปิดจุดบริการ ไม่ใช่ deadline ต่อขั้นตอน) |
| FR-06 | ลงทะเบียนรายวัน: เลือกแม่แบบ/ไทย-ต่างชาติ/เดินได้-รถเข็น สร้างขั้นตอนอัตโนมัติ (1 Visit ที่ใช้งานอยู่ต่อคนต่อวัน) | 🟡 | ฟอร์มลงทะเบียน (`RegisterVisitForm.tsx`) ทำงานได้ครบ, สร้าง `VisitStep` snapshot อัตโนมัติจากแม่แบบ, มี checkbox รถเข็น (`uses_wheelchair`) แต่ "ไทย/ต่างชาติ" ถูกแทนด้วย `preferred_language` (th/en) ของผู้ป่วยแทน ไม่ใช่ flag สัญชาติตรง ๆ — ตอบโจทย์เรื่องภาษาได้ แต่ความหมายไม่ตรงตัวสเปก และยังไม่พบการบังคับ "1 Visit ที่ใช้งานอยู่ต่อคนต่อวัน" (ไม่มี unique constraint/validation ป้องกันลงทะเบียนซ้ำวันเดียวกัน) |
| FR-07 | ออกใบเวชระเบียนพร้อม QR ให้ผู้ป่วยดูสถานะตนเอง | 🟡 | `VisitQrCodeButton.tsx` สร้าง QR (`{origin}/visit/{qr_token}`) และเปิดหน้าต่างพิมพ์ได้ แต่ `qr_token` **ไม่หมดอายุเมื่อ visit เป็น COMPLETED/CANCELLED** (endpoint `visit_by_token` ไม่ตรวจ status ก่อนคืนข้อมูล) ซึ่งขัดกับที่ระบุไว้ใน `MODELS.md` เอง |
| FR-08 | แสดงขั้นตอนทั้งหมดพร้อมสีสถานะ, "ขั้นตอนที่ n จาก m", บอกจุดถัดไปพร้อมเหตุผลเมื่อยังระบุไม่ได้ | 🟡 | สีสถานะ (เทา/ฟ้า/ส้ม/เขียว/ขีดฆ่า) และ "จุดถัดไป" (`NextStepCard`) ทำแล้วครบ แต่ยังไม่พบข้อความรูปแบบ "ขั้นตอนที่ n จาก m" ที่ชัดเจน และไม่มี "เหตุผล" เมื่อจุดถัดไปยังระบุไม่ได้ (เช่น "รอผลตรวจ") เพราะไม่มี step-type "รอ" (ดู FR-05) |
| FR-09 | แสดงคิว/เวลารอ, เลือกไปจุดใดก่อนได้เมื่อมีหลายจุด, แนะนำจุดเวลาจำกัด/คิวสั้นกว่า, ไม่โชว์ชื่อ/โรคคนอื่น | 🟡 | Queue widget แสดงหมายเลขคิว/จำนวนคนก่อนหน้าได้ (`QueueWidget.tsx`) และ NextStepOptionsList แสดงตัวเลือกคู่ขนานได้ แต่ **ไม่มี logic "แนะนำ" จุดที่ควรไปก่อน** (ไม่มีเวลาจำกัดให้เทียบ, ไม่เปรียบเทียบความยาวคิวระหว่างตัวเลือก) — ผู้ป่วยเห็นตัวเลือกแต่ระบบไม่ช่วยตัดสินใจ |
| FR-10 | แจ้งเตือนผู้ป่วยเมื่อใกล้ถึงคิว | ❌ | ไม่มี `Notification` model และไม่มีกลไก push/แจ้งเตือนใด ๆ ในโค้ด (หน้าผู้ป่วย polling ทุก 10 วินาทีเท่านั้น ไม่มี alert เชิงรุก) |
| FR-11 | เตือนเมื่อขั้นตอนเวลาจำกัดใกล้หมดเวลา (ทั้งฝั่งผู้ป่วยและฝั่งเจ้าหน้าที่) | ❌ | ขึ้นกับ FR-05 ที่ยังไม่มีฟิลด์เวลาบังคับ จึงไม่มีอะไรให้เตือน |
| FR-12 | นำทางเป็นคำสั่งเข้าใจง่าย (เลี้ยว/ลิฟต์/ชั้น) พร้อมระยะทาง/เวลาเดิน | ❌ | สิ่งที่มีจริงคือ **เข็มทิศบอกทิศทาง + ระยะตรงเป็นเส้นตรง** (`direction.ts`, `DirectionBlock.tsx`) คำนวณจากพิกัด x,y เท่านั้น ไม่ใช่คำสั่งเดิน ("เลี้ยวซ้าย", "ขึ้นลิฟต์ไปชั้น 3") และไม่ได้เดินตามเส้นเชื่อมจริงในผัง (ไม่สนใจกำแพง/เส้นทาง) |
| FR-13 | คำนวณเส้นทางสั้นสุดจากฐานข้อมูลผัง (ไม่ฝังเส้นทาง) เคารพทิศทางเส้นเชื่อมและลิฟต์จอดบางชั้น | ❌ | **ไม่มี pathfinding algorithm ในโค้ดเลย** (ไม่พบ Dijkstra/BFS/A* ทั้งฝั่ง backend/frontend) ฟิลด์ `is_bidirectional`, `wheelchair_accessible`, `walk_time_sec` มีไว้เป็น "ข้อมูลรอ routing engine มาอ่าน" แต่ยังไม่มี engine ใดอ่านเลย มีเอกสารภายใน (`IMPLEMENT_PLAN.md`) ยืนยันเองว่านี่คือ scope ที่ยังไม่ทำ — **นี่คือ gap สำคัญที่สุดของระบบทั้งหมด เพราะ FR หลายตัว (03, 11-ส่วนเตือน, 12, 15, 17, 18) พึ่งพา routing engine ตัวนี้ทั้งสิ้น** |
| FR-14 | ระบุตำแหน่งปัจจุบันด้วยสแกน QR (ไม่ใช้ GPS) | ✅ | `location_qr_code` บน `Node` + กล้องสแกน (`CameraScanner.tsx`/jsQR) หรือกรอกโค้ดเอง ใช้ได้ทั้งหน้าผู้ป่วยและ kiosk |
| FR-15 | แสดงแผนผังชั้นพร้อมลากเส้นทางที่ต้องเดิน | ❌ | แผนผังชั้น (floor plan image) มีเฉพาะใน **Admin editor** เพื่อวางจุด/เส้นเชื่อม ไม่ได้แสดงให้ผู้ป่วย/kiosk เห็น และไม่มีการวาดเส้นทางทับภาพเลย |
| FR-16 | สลับ TH/EN ทั้งระบบ | ✅ | `next-intl` + `messages/th.json`/`en.json` ครบทั้ง Admin และ Patient/Kiosk (ภาษาตั้งต้นตาม `preferred_language` ของผู้ป่วย) |
| FR-17 | ขยายตัวอักษร + โหมดเลี่ยงบันไดอัตโนมัติสำหรับผู้ป่วยรถเข็น | ❌ | ไม่มีปุ่ม/การตั้งค่าขยายตัวอักษรที่ใดเลย และ `uses_wheelchair` แม้เก็บไว้ในข้อมูลผู้ป่วยแล้ว แต่ไม่มีการใช้งานจริง (ไม่มี routing engine ให้เลี่ยงบันได — ดู FR-13) |
| FR-18 | คำนวณเส้นทางใหม่และแจ้งผู้ป่วยเมื่อเส้นทางถูกกระทบจากปิดลิฟต์/เส้นเชื่อม | ❌ | ขึ้นกับ FR-03 + FR-13 ซึ่งทั้งคู่ยังไม่มี |
| FR-19 | หน้าจอเจ้าหน้าที่ประจำจุด: ดูคิว, check-in, เรียกคิว/เรียกซ้ำ, บันทึกเสร็จ (รวมผลตรวจพร้อม), **ระบุจุดถัดไป**, ผู้ป่วยไม่มา, ข้ามขั้นตอน (ต้องระบุเหตุผล) | 🟡 | ดูคิว/เรียกคิวถัดไป/Serve/Done ทำแล้ว (`QueueConsolePage.tsx`) แต่: **(1) ไม่มีปุ่ม "ระบุจุดถัดไป" ให้เจ้าหน้าที่เลือกเองเลย** — ระบบให้ "จุดถัดไป" มาจาก prerequisite ของแม่แบบอัตโนมัติเท่านั้น จึงไม่ตรงกับ workflow ในสเปกที่ตั้งใจให้ "เจ้าหน้าที่เป็นผู้ระบุจุดถัดไป" ในทุกจุด (ดู Flow Diagram ในเอกสาร event #1,3,6,11,13); **(2) ไม่มี "เรียกซ้ำ" แยกจากเรียกคิวครั้งแรก**; **(3) "ผู้ป่วยไม่มา" ไม่มีสถานะ/ปุ่มแยกจาก skip ทั่วไป**; **(4) ปุ่ม Skip ไม่มีการบังคับ/บันทึกเหตุผล** (ไม่มีฟิลด์ reason ใน model ใดเลย) |
| FR-20 | เพิ่มขั้นตอนกลางคัน (เช่น X-ray ปอด) พร้อมโชว์จำนวนคิวจุดนั้น โดยไม่ทำให้เงื่อนไขก่อนหลังเสีย | ❌ | `VisitStep.is_planned=False` มีไว้ในโมเดลรองรับ concept นี้ และ badge "ad hoc" render ได้ **แต่ไม่มี UI หรือ API ใดสร้างขั้นตอนแบบนี้ได้จริง** — เป็นแค่ field เปล่าที่ยังไม่ถูกใช้ |
| FR-21 | ตรวจเงื่อนไขก่อนหลังตอนระบุจุดถัดไป, check-in ได้เฉพาะจุดที่ถูกระบุ | 🟡 | การตรวจ prerequisite ก่อน start step มีจริง (`visits/views.py`) แต่เพราะ "การระบุจุดถัดไปโดยเจ้าหน้าที่" (FR-19/20) ยังไม่มี UI จึงยังตรวจไม่ได้ในบริบทที่สเปกตั้งใจไว้ — ระบบปัจจุบันให้ผู้ป่วย/เจ้าหน้าที่ start ได้ทุก step ที่ prerequisite ผ่านพร้อมกัน ไม่ได้ "จำกัดเฉพาะจุดที่ระบุ" ตามที่ FR ต้องการ |
| FR-22 | Login+สิทธิ์ตามบทบาท (hash, prepared statement) + ผู้ป่วยเห็นเฉพาะข้อมูลตน | 🟡 | Hash รหัสผ่านและใช้ Django ORM ล้วน (ไม่มี raw SQL) ทำถูกต้อง — **แก้แล้ว:** เพิ่ม `RoleRequired` permission class (`backend/accounts/permissions.py`) และตั้ง `read_roles`/`write_roles` ตามตาราง Role ของสเปกให้ทุก `ModelViewSet`/`ReadOnlyModelViewSet` ใน `accounts`, `facility`, `pathway`, `visits`, `queues` แล้ว, เปลี่ยน `DEFAULT_PERMISSION_CLASSES` เป็น `IsAuthenticated` (`config/settings.py`) เป็น fail-safe backstop, และ endpoint ที่ตั้งใจให้ public (login/logout/me/health/kiosk lookup/visit-by-token/visit-by-hn-today/location-node-by-qr) ยังคง `AllowAny` ตามเดิม (ผู้ป่วยเข้าถึงข้อมูลตนผ่าน token เหล่านี้ ไม่ใช่ session login จึงไม่ได้แก้ในรอบนี้). ยังมี simplification ที่ตั้งใจปล่อยไว้ 2 จุด: (1) สเปกบางแถวให้ role ต่างกันระหว่าง `list` กับ `retrieve` ของ resource เดียวกัน (เช่น `GET /api/visits/visits` ให้ REGISTRAR+EXECUTIVE แต่ `GET .../{id}` ให้ REGISTRAR อย่างเดียว) — `RoleRequired` ปฏิบัติ list/retrieve เหมือนกันหมดโดยเจตนา ไม่ถือเป็นช่องโหว่ที่มีผลจริง; (2) ยังไม่ทำ per-service-point scoping (SERVICE_STAFF เห็นคิวของทุกจุดบริการ ไม่ใช่เฉพาะจุดที่ตนสังกัดผ่าน `ServicePointStaff`) — เป็น role-level gating เท่านั้น ยังไม่ scope ลงไปถึงระดับ object |
| FR-23 | บันทึก Audit ทุกการเปลี่ยนสถานะ: ใคร/เมื่อไร/จากสถานะใด→เป็นสถานะใด (เวลาระบบ) | 🟡 | มี `AuditLog` + `log_action()` จริง ครอบคลุม LOGIN/LOGOUT, CREATE/UPDATE/DELETE staff user, START/COMPLETE/SKIP visit step — แต่ **ไม่มีฟิลด์ from_status/to_status แยกชัดเจน** (เก็บเป็น JSON `detail` อิสระ ไม่ครบทุกครั้ง) และ **ไม่ครอบคลุม** การแก้ไข Facility/Pathway/ServiceSchedule/ServicePointStaff, Patient/Visit CRUD, และปุ่ม serve/done ในหน้า Queue Console (เรียก `complete_step` แต่ log เฉพาะบางเส้นทาง) |
| FR-24 | Dashboard ผู้บริหาร: เวลารอเฉลี่ยรายจุด, จุดคอขวด, เวลาที่ผู้ป่วยอยู่ รพ. เฉลี่ย (ไม่ระบุตัวผู้ป่วย) | 🟡❗ | มีหน้า `/admin/reports` แต่คำนวณเฉพาะ **ค่าเฉลี่ยรวมทั้งโรงพยาบาล** ไม่ได้แยกรายจุดบริการ (ไม่ตอบ "เวลารอเฉลี่ยรายจุด"), **ไม่มี bottleneck view** ใด ๆ, และ **ไม่มีตัวเลข "เวลาที่ผู้ป่วยอยู่ รพ. เฉลี่ย"** (มีแค่ "จำนวนขั้นตอนเฉลี่ยต่อ visit" ซึ่งเป็นคนละเรื่อง) ทุกอย่างคำนวณฝั่ง client จาก list ทั้งหมด ไม่มี aggregation API หลังบ้าน (ประสิทธิภาพจะแย่เมื่อข้อมูลมาก ขัด NFR-05) นอกจากนี้ **หน้า `/admin` (Dashboard ภาพรวม) ที่ Executive เข้าถึงได้ กลับโชว์ชื่อ-นามสกุลผู้ป่วยในตาราง "รายการเข้ารับบริการล่าสุด"** ซึ่งขัดกับข้อกำหนด "ไม่ระบุตัวผู้ป่วย" ของบทบาทผู้บริหารตรง ๆ |
| FR-25 (Could/Release 1.1) | จัดลำดับขั้นตอนใหม่อัตโนมัติเมื่อคิวยาวผิดปกติ | ❌ | ไม่มีเลย (ตามคาด เพราะเป็น Release 1.1) |
| FR-26 (Could/Release 1.1) | ญาติติดตามผ่านลิงก์อายุจำกัด | ❌ | ไม่มีเลย |
| FR-27 (Could/Release 1.1) | อ่านคำสั่งนำทางเป็นเสียง | ❌ | ไม่มีเลย |
| FR-28 (Could/Release 1.1) | แนะนำจุดพัก/ห้องน้ำ/ร้านอาหารระหว่างทาง | ❌ | ไม่มีเลย |

---

## 2. Non-Functional Requirements (NFR)

| ID | เรื่อง | สถานะ | รายละเอียด |
|---|---|---|---|
| NFR-01 | Hash password, Prepared Statement, ตรวจสิทธิ์ทุก API | 🟡 | Hash ✅ / ORM (ไม่มี raw SQL) ✅ / **"ตรวจสิทธิ์ทุก API" แก้แล้ว** — ทุก ViewSet ตรวจสิทธิ์ผ่าน `RoleRequired` (`backend/accounts/permissions.py`) และ default ที่เหลือ (ถ้ามีใครลืมตั้ง) เป็น `IsAuthenticated` ไม่ใช่ `AllowAny` แล้ว ดูรายละเอียด simplification ที่เหลือใน FR-22 |
| NFR-02 | Audit ทุกการเปลี่ยนสถานะสำคัญ แก้/ลบไม่ได้ | 🟡 | มี audit log แต่ยังไม่ครอบคลุมทุก action (ดู FR-23) และ "แก้/ลบไม่ได้" เป็นเพียง **convention** (ไม่เรียก update/delete ในโค้ด) ไม่มี DB constraint/trigger บังคับจริง — ผู้ที่เข้าถึง DB โดยตรงยังลบ/แก้ได้ |
| NFR-03 | ใช้งานง่ายไม่ต้องอบรม, error message เข้าใจง่าย, ไม่โชว์ error code ดิบ | ⚪ ไม่ได้ตรวจสอบในรอบนี้ | ต้องทดสอบ UI จริงเพื่อยืนยัน ยังไม่ได้ตรวจ error-handling ของทุก endpoint |
| NFR-04 | ขยายตัวอักษรได้, contrast ≥ 4.5:1, ใช้บนจอ 320px ได้, ไม่ใช้สีอย่างเดียวบอกสถานะ | ❌ | ไม่มีปุ่ม/กลไกขยายตัวอักษรหรือโหมด high-contrast เลย, ไม่มีการตรวจ/ยืนยันอัตราส่วนสี, สถานะบางจุด (เช่น timeline) ใช้สีคู่กับ label ข้อความอยู่บ้าง (ผ่านเกณฑ์ "ไม่ใช้สีอย่างเดียว" บางส่วน) แต่ไม่ครบทุกจุดและไม่มีการทดสอบจอ 320px |
| NFR-05 | หน้าจอหลักแสดงผลภายใน 3 วินาที | ⚪ ไม่ได้ตรวจสอบในรอบนี้ | มีความเสี่ยง: Dashboard/Reports ใช้ pattern ดึงข้อมูลทีละ queue (N+1) และคำนวณฝั่ง client จาก list ทั้งหมด — ควรวัดจริงเมื่อข้อมูลจำลองมีขนาดใกล้เคียงของจริง |
| NFR-06 | Responsive ทั้งมือถือ/แท็บเล็ต/คอมพิวเตอร์ | 🟡 | หน้าผู้ป่วย/Kiosk เป็น layout กว้างคงที่ (`max-w-[430px]`) ใช้งานบนมือถือได้จริงแต่ไม่ปรับตาม breakpoint จริง ๆ; **ฝั่ง Admin เป็น desktop-only** (sidebar กว้างคงที่ ไม่มี responsive collapse) — ถ้าเจ้าหน้าที่ประจำจุดต้องใช้แท็บเล็ต/มือถือจะมีปัญหาการใช้งาน |
| NFR-07 | DB เชิงสัมพันธ์ถึง 3NF, แยก Frontend/Backend ผ่าน REST API มีเอกสาร endpoint+ตัวอย่าง | ✅ | Django ORM + DRF, Next.js แยกส่วนชัดเจนผ่าน REST, มี `drf-spectacular` (Swagger UI/Redoc) เป็นเอกสาร API พร้อมตัวอย่าง โครงสร้างโมเดลดูเป็น normalized form ที่สมเหตุสมผล |
| NFR-08 | อธิบายแนวทางสำรอง/กู้คืนข้อมูลได้ (ไม่ต้องทำงานจริง) | ❌ | ไม่พบเอกสารเรื่อง backup/restore ในโปรเจกต์เลย (ตรวจ README, IMPLEMENT_PLAN.md, seed/README.md, CREDENTIALS.md แล้วไม่มี) |
| NFR-09 | ใช้ Git commit สม่ำเสมอ, ใช้ข้อมูลจำลองเท่านั้น, ระบุการใช้ AI ในรายงาน | 🟡 | มี git history ต่อเนื่อง (`git log`) และมีชุด seed data จำลอง (`seed/*.csv` + loader) ตรงตามเจตนา; "ระบุการใช้ AI ในรายงาน" เป็นเนื้อหาที่ต้องเขียนแยกในรายงานส่ง ไม่ใช่สิ่งที่ตรวจจากโค้ดได้ |

---

## 3. API Contract (หมวด `4. Requirements` และเอกสาร API Contract)

โครงสร้างแอป (`accounts`, `facility`, `pathway`, `visits`, `queues`, `health`) และ endpoint ส่วนใหญ่ตรงกับที่ระบุในสเปกจริง (CRUD ครบตามลิสต์ endpoint, มี `by-token`/`by-hn-today` สำหรับผู้ป่วย/kiosk, มี `call-next`/`serve`/`done` สำหรับคิว) แต่มีความคลาดเคลื่อนหลักคือ:

- ~~คอลัมน์ "Role" ในเอกสาร (เช่น ADMIN, REGISTRAR, SERVICE_STAFF, EXECUTIVE) เป็นเพียงเจตนาการออกแบบ ไม่ได้ถูก enforce จริงที่ backend~~ **แก้แล้ว** — ทุก ViewSet ตั้ง `read_roles`/`write_roles` ตามคอลัมน์ Role ของสเปกและตรวจผ่าน `RoleRequired` (`backend/accounts/permissions.py`) แล้ว ดูรายละเอียด/simplification ที่เหลือใน FR-22/NFR-01
- Endpoint สำหรับ "ระบุจุดถัดไปโดยเจ้าหน้าที่" (ตามที่ FR-19/20/21 ต้องการ) **ไม่มีอยู่ในเอกสาร API Contract และไม่มีในโค้ดจริงด้วย** — สอดคล้องกันแต่ทั้งสองฝั่งขาดฟีเจอร์นี้เหมือนกัน
- ไม่มี endpoint ใดเกี่ยวกับ routing/pathfinding (สมเหตุสมผล เพราะ engine นี้ยังไม่มี — ดู FR-13)
- `GET /api/health` มีจริง (`core/views.py`, `core/urls.py`)

---

## 4. ส่วนที่เป็นไดอะแกรม/รูปภาพในเอกสาร (ตรวจไม่ได้จากข้อความ)

หัวข้อต่อไปนี้ในเอกสารมีเฉพาะหัวเรื่อง ไม่มีเนื้อหาข้อความให้ดึงออกมาได้ (คือรูปภาพ/diagram ที่ฝังไว้): **Use Case Diagram, ER Diagram, UX/UI Wireframe/Mockup, Test Case** — ควรเปิดไฟล์ต้นฉบับดูภาพเพื่อเทียบเพิ่มเติม โดยเฉพาะ **Test Case** ซึ่งควรใช้ตรวจว่ามี automated/manual test coverage ในโค้ดตรงกันหรือไม่ (จากการสำรวจโค้ด ยังไม่พบชุดทดสอบอัตโนมัติที่ครอบคลุม flow หลักในโปรเจกต์)

---

## 5. เรียงลำดับความสำคัญที่ควรแก้ก่อน (แนะนำ)

1. ~~บังคับสิทธิ์ตามบทบาทที่ระดับ API (FR-22 / NFR-01)~~ **แก้แล้ว** — ดูรายละเอียดใน FR-22/NFR-01
2. **Pathfinding engine (FR-13)** — เป็นฐานของฟีเจอร์นำทางแทบทั้งหมด (FR-03, 11, 12, 15, 17, 18) ถ้าไม่ทำ ฟีเจอร์นำทางที่ทำได้จริงจะเหลือแค่เข็มทิศ ไม่ตรงกับสเปก M5/M6
3. **Workflow "เจ้าหน้าที่ระบุจุดถัดไป" + เพิ่มขั้นตอนกลางคัน (FR-19–21, FR-20)** — เป็น M7 Must Have ที่ตอนนี้ยังไม่มี UI/API เลย เป็น core flow ของสถานการณ์ตัวอย่างในเอกสาร (Flow Diagram) ทั้งหมด
4. **Dashboard แยกรายจุด + ซ่อนชื่อผู้ป่วยจากมุมมอง Executive (FR-24)**
5. Cycle detection บนแม่แบบ, skip-with-reason, no-show แยกจาก skip, audit ให้ครอบคลุมทุก action, backup/restore doc, accessibility (font-scale/contrast) — ทำเป็นชุดถัดไปได้หลังแก้ 4 ข้อบน
